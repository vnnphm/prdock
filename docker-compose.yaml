services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: prdock
      POSTGRES_PASSWORD: 67676767
      POSTGRES_DB: prdock

    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
